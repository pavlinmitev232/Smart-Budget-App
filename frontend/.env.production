# Frontend Environment Variables for Production
# Create this file as .env.production when building for production

# API URL - in production, use relative path since Nginx proxies /api
VITE_API_URL=/api
