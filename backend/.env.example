# ================================================
# Smart Budget App - Backend Configuration
# ================================================
# Copy this file to .env and update with your values

# ---- Server Configuration ----
# Port for the backend API server
PORT=5000

# Environment: development | production | test
NODE_ENV=development

# ---- Database Configuration ----
# PostgreSQL connection settings
# You can use either DATABASE_URL or individual DB_* variables

# Option 1: Connection URL (recommended)
DATABASE_URL=postgresql://smartbudget:dev_password_123@localhost:5432/smart_budget

# Option 2: Individual connection parameters
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_budget
DB_USER=smartbudget
DB_PASSWORD=dev_password_123

# ---- Authentication (Future Use) ----
# JWT secret for token signing (will be required in Story 2.x)
# IMPORTANT: Use a strong random string in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# JWT_SECRET=your_jwt_secret_key_here_change_in_production

# ================================================
# Notes:
# - Never commit the actual .env file to version control
# - Use strong passwords in production
# - DATABASE_URL takes precedence over individual DB_* variables
# ================================================
